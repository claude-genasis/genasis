# CLAUDE.md

@import GENASIS.md

<!--
Genasis generated this stub on first `attach` because no CLAUDE.md
was present in the project root. The `@import GENASIS.md` line above
is the only thing Claude Code needs to load the protocol contract +
slash commands + hooks that `genasis attach` installed under
`.claude/genasis/`. Without that line, none of those activate.

Add any project-specific guidance below — coding conventions,
domain glossary, file-path shortcuts, anything you'd otherwise be
copy-pasting into prompts. Claude Code reads this file at every
session start and treats it as the closest thing to "always-on"
context for the project.

`genasis attach` will NOT overwrite this file on re-runs (the import
line stays in place because it's the conventional path; you can move
or split it but the contract is satisfied as long as GENASIS.md is
imported somewhere Claude Code will see).
-->
