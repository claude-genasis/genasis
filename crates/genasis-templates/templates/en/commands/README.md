# Slash command templates

Each `*.md.tera` becomes a file in `.claude/genasis/commands/` of the user's
project. M6.

Initial set:
`sprint-start, sprint-end, sprint-status, sprint-rescue, sprint-reconcile,
intake-review, issue-link, issue-done, issue-block, design-change,
db-migrate, db-status, agent-handoff, agent-resume, check-inbox,
record-progress` (16 total).
