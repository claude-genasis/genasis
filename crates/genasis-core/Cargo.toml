[package]
name = "genasis-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Core utilities: config, env, fs, marker fence, error types"

[dependencies]
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
sha2.workspace = true
hex.workspace = true
dirs.workspace = true
indexmap.workspace = true
regex.workspace = true
tracing.workspace = true
chrono.workspace = true
# D-104: slug abbreviation for `genasis provision`. `deunicode` handles
# Hangul → ASCII transliteration when the translation path (Claude CLI)
# is unavailable; `slug` provides a base ASCII slugify used as the final
# kebab-case normalization step.
slug = "0.1"
deunicode = "1.4"
which = "6"

[dev-dependencies]
tempfile.workspace = true
