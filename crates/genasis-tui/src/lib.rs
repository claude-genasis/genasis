//! genasis-tui — shared Ratatui components.
//!
//! Used by both the install/attach TUI (genasis-cli/tui_attach) and the
//! runtime dashboard (genasis-monitor). M9.

pub mod layout;
pub mod theme;
pub mod widgets;
pub mod wizard;
